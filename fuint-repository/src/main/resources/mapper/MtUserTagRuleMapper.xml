<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fuint.repository.mapper.MtUserTagRuleMapper">

    <select id="getMerchantRuleList" resultType="com.fuint.repository.model.MtUserTagRule">
        SELECT * FROM mt_user_tag_rule
        WHERE MERCHANT_ID = #{merchantId}
        <if test="status != null and status != ''">
            AND STATUS = #{status}
        </if>
        ORDER BY PRIORITY DESC, ID DESC
    </select>

    <select id="getAutoRuleList" resultType="com.fuint.repository.model.MtUserTagRule">
        SELECT * FROM mt_user_tag_rule
        WHERE MERCHANT_ID = #{merchantId}
        AND IS_AUTO = 'Y'
        AND STATUS = 'A'
        ORDER BY PRIORITY DESC, ID DESC
    </select>

    <select id="getRulesByTagId" resultType="com.fuint.repository.model.MtUserTagRule">
        SELECT * FROM mt_user_tag_rule
        WHERE TAG_ID = #{tagId} AND STATUS = 'A'
        ORDER BY PRIORITY DESC
    </select>

</mapper>
