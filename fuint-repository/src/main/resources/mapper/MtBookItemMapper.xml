<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fuint.repository.mapper.MtBookItemMapper">
    <select id="getBookList" resultType="java.lang.String">
        SELECT SERVICE_TIME FROM mt_book_item t WHERE t.BOOK_ID = #{bookId} AND t.STATUS !='D'
        <if test="date != null and date != ''">
            AND t.SERVICE_DATE = #{date}
        </if>
        <if test="time != null and time != ''">
            AND t.SERVICE_TIME = #{time}
        </if>
    </select>
    <select id="getBookItemByUserId" resultType="com.fuint.repository.model.MtBookItem">
        SELECT t.* FROM mt_book_item t WHERE t.BOOK_ID = #{bookId} AND t.USER_ID = #{userId} AND t.STATUS !='D'
        <if test="date != null and date != ''">
            AND t.SERVICE_DATE = #{date}
        </if>
        <if test="time != null and time != ''">
            AND t.SERVICE_TIME = #{time}
        </if>
        LIMIT 1
    </select>
</mapper>
